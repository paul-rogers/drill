{
    head:{
        type:"APACHE_DRILL_PHYSICAL",
        version:"1",
        generator:{
            type:"manual"
        }
    },
    graph:[
        {
            @id:1,
            pop:"mock-sub-scan",
            entries:[
              {records: 100, types: [
                {name: "blue", type:{type: "INT", mode: "REPEATED"}},
                {name: "red", type:{type: "BIGINT", mode: "REPEATED"}},
                {name: "green", type:{type: "INT", mode: "REQUIRED"}}
              ]}
            ]
        },
        {
            @id:2,
            child: 1,
            pop:"project",
            exprs: [
              { ref: "cnt", expr:"repeated_count(blue)" },
              { ref: "has_min", expr:"repeated_contains(red, 9223372036854775807)" }
            ]
        },
        {
            @id: 3,
            child: 2,
            pop: "screen"
        }
    ]
}
