drill.exec: {
  cluster-id: "drillbits1",
  sort: {
    purge.threshold : 100,
    external: {
      batch.size : 4000,
      //batch_limit: 200,
      disable_managed: true
      //mem_limit: 20000000,
      spill: {
        //batch.size : 4000,
        group.size : 50,
        threshold : 50,
        directories : [ "/tmp/drill/spill" ],
        fs : "file:///"
      }
    }
  },

  memory: {
    top.max: 10000000,
    operator: {
      max: 200000,
      initial: 100
    },
    fragment: {
      max: 200000,
      initial: 200
    }
  },

  zk.connect: "localhost:2181"
}
