drill.exec: {
  cluster-id: "drillbits1",
 sort: {
    purge.threshold : 100,
    external: {
      batch.size : 4000,
      spill: {
        batch.size : 4000,
        group.size : 100,
        threshold : 200,
        directories : [ "/tmp/drill/spill" ],
        fs : "file:///"
      }
    }
  },

  memory: {
    top.max: 10000000,
    operator: {
      max: 200000,
      initial: 100
    },
    fragment: {
      max: 200000,
      initial: 200
    }
  },

  zk.connect: "localhost:2181"
}
