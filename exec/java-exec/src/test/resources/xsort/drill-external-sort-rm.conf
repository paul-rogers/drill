drill.exec: {
  cluster-id: "drillbits1",
  sort: {
    purge.threshold : 100,
    external: {
      batch.size : 4000,
      batch_limit: 50,
      merge_limit: 50,
      //mem_limit: 10000000,
      spill: {
        //min_batches: 200,
        max_batches: 50,
        batch.size : 4000,
        group.size : 100,
        threshold : 200,
        directories : [ "/tmp/drill/spill" ],
        fs : "file:///"
      }
    }
  },

  memory: {
    top.max: 10000000,
    operator: {
      max: 200000,
      initial: 100
    },
    fragment: {
      max: 200000,
      initial: 200
    }
  },

  zk.connect: "localhost:2181"
}
